using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sckp2 : MonoBehaviour
{
/*    public static sckp2 instance;
    public int collectableScore = 0;
    public int totalScore = 0;
    public int remainingCount = 4;
    public TextMeshProUGUI colText;
    public TextMeshProUGUI remText;
    public TextMeshProUGUI totalText;

    // Update is called once per frame
    private void Awake()
    {
        instance = this;
    }

    void Update()
    {
        colText.text = "Hyper cubes: " + collectableScore.ToString();
        eneText.text = "Remaining: " + remainingCount.ToString();
        if (totalScore >= 0 && totalScore < 100)
            totalText.text = "Score: " + totalScore.ToString();
        if (totalScore == 20)
            totalText.text = "VICTORY";
    }

    public void colScore()
    {
        collectableScore++;
        remainingCount--;
        totalScore += 5;
    }
    public void enemyDown()
    {
        enemyCount--;
        totalScore += 20;
    }
    public bool defeat()
    {
        if (totalScore < -1)
        {
            return true;
        }
        return false;
    }
    public void hit()
    {
        totalScore -= 100;
    }

    public bool colSuccess()
    {
        if (collectableScore == 0) { return true; }
        return false;
    } */
}
